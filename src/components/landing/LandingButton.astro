---
interface Props {
  text: string;
  color: string;
}
const { text, color } = Astro.props as Props;

const buttonStyle = {
    backgroundColor: color,
  };
---
<div>
  <button class="text-white text-center px-5 py-2 rounded-lg whitespace-nowrap flex justify-center items-center" style={buttonStyle}>
      <p>{text}</p></button>
    </div>
 